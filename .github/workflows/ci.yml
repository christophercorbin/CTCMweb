name: CI

# Quality gates that run on every PR + push to main/develop.
# Deploys are handled by Amplify Hosting's webhook (see amplify.yml).
# Configure Amplify Hosting → App settings → "Require CI status" if you
# want a green CI run to be a precondition for the backend deploy.

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  # Undrained list() queries silently drop records once a table outgrows one
  # page — the failure that hid 54% of customer invoices from admins. Needs no
  # AWS credentials or amplify_outputs.json: it is a static source scan.
  pagination:
    name: Pagination Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with: { node-version: 22, cache: npm }
      - run: node scripts/check-pagination.mjs

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run lint --workspaces --if-present

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with: { node-version: 22, cache: npm }
      - run: npm ci
      # amplify_outputs.json is gitignored (generated per-branch by Amplify
      # Hosting). Stub it so the static import in apps/web/src/lib/amplify.ts
      # resolves at typecheck time. Covers BOTH apps/web AND amplify/ workspaces
      # (Lambda TS errors surface here instead of in the expensive pipeline-deploy).
      - run: echo '{"version":"1"}' > amplify_outputs.json
      - run: npm run typecheck --workspaces --if-present

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with: { node-version: 22, cache: npm }
      - run: npm ci
      # Stub the gitignored, per-branch amplify_outputs.json so component tests
      # that transitively import apps/web/src/lib/amplify.ts can resolve it.
      - run: echo '{"version":"1"}' > amplify_outputs.json
      # Runs Vitest in every workspace that defines a "test" script
      # (apps/web via jsdom + Testing Library, amplify via node).
      - run: npm run test --workspaces --if-present
